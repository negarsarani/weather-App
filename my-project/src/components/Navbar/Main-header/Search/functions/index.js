export function LocalStorage(items) {
  const createStorage = localStorage.setItem('Items', `${items}`);
}

export function localStoragepp(){
    const myValue = localStorage.getItem("myKey");
}